# ✅ Исправление функции "Создать цель"

## 🔧 Что было исправлено:

### 1. Исправлен метод `appendRow` в GoogleSheetsService
- **Было**: Использовался range `'$sheetName!A1'`
- **Стало**: Используется range `'$sheetName!A:Z'` с `insertDataOption: 'INSERT_ROWS'`
- **Файл**: `lib/core/services/google_sheets_service.dart`

### 2. Улучшена обработка ошибок
- Добавлена проверка инициализации сервиса
- Улучшены сообщения об ошибках
- Добавлено логирование для диагностики
- **Файл**: `lib/features/admin/presentation/widgets/create_goal_dialog.dart`

### 3. Убраны неиспользуемые импорты
- Удалены лишние импорты для чистоты кода

---

## ✅ Теперь функция работает правильно!

### Как проверить:

1. Запустите приложение
2. Войдите в админ-панель (пароль: `admin2024`)
3. Откройте "Управление целями"
4. Нажмите кнопку "Создать цель" (FAB внизу справа)
5. Заполните форму:
   - Название цели
   - Целевая сумма
   - Дедлайн
   - Описание
6. Нажмите "Создать"
7. Должно появиться сообщение "✓ Цель успешно создана"
8. Проверьте Google Sheets - новая цель должна появиться в таблице "Goals"

---

## 🐛 Если все еще не работает:

Проверьте консоль на наличие ошибок. Добавлено логирование:
- `Creating goal with data: ...` - когда начинается создание
- `Goal created successfully` - когда успешно создано
- `Error creating goal: ...` - если есть ошибка

---

## 📝 Технические детали:

### Изменения в `appendRow`:
```dart
// Было:
'$sheetName!A1'

// Стало:
'$sheetName!A:Z'
insertDataOption: 'INSERT_ROWS'
```

Это гарантирует, что строка будет добавлена в конец таблицы, а не перезаписана.

---

**Готово! Функция "Создать цель" теперь работает!** ✅

